'''
random


random is a library for generating random numbers and sequences in Python.
It provides functions for generating random integers,
selecting random elements from a sequence, shuffling sequences, and more.

Example use case: Generate a random number between 1 and 100.

'''

import random

number = random.randint(1, 100)

print(number)

